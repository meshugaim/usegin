# inbox/

Raw verbatim capture from Lihu, before triage. One file per pour-session, or
per pour, depending on density.

Naming: `<YYYY-MM-DD>-<short-slug>.md` — slug is just enough to recognize the
content later (e.g. `2026-04-27-zisser-build`, `2026-04-27-bug-oauth`).

## Posture

- **Verbatim first.** Don't paraphrase before placing.
- **Triage same-turn when you can.** When a pour clearly fits a route, place
  it directly and skip `inbox/`. Use `inbox/` for the unclear ones.
- **Don't accumulate.** When `inbox/` has more than a handful of un-triaged
  files, that's a friction signal — Zisser is dropping the loop. Triage.

(Open-to-empty per z003. The folder has worth even when empty.)
