---
name: initializing-bun-projects
description: Use this skill when creating new Bun projects, modules, or CLI tools. Triggered by "bun init", "new bun project", "new module", "new cli tool".
---

# Initializing Bun Projects

```bash
mkdir <project-name> && cd <project-name> && bun init
```

Use Context7 MCP (`mcp__context7__get-library-docs`) for latest Bun APIs before implementing.
