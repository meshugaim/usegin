### 2026-03-13 — Run 20260313_140120_eng-2004 (ENG-2004: Linear integration, run 6)
**Verdict:** collapsed
**Collapse events:** 2

**Key observations:**

- **Signal protocol: collapse — false completion detection.** The run ended after 1 session with `outcome=complete`, but the agent output `AUTO_IMPLEMENT_HANDOFF` (not COMPLETE). Three slices remain unimplemented (ENG-2582, ENG-2583, ENG-2584). The handoff file explicitly lists them as pending. **Root cause:** `detectSignal()` in `run.ts:97-101` does `stdout.includes("AUTO_IMPLEMENT_COMPLETE")`. With `--output-format stream-json`, stdout contains the full conversation stream — including the prompt text (which contains `AUTO_IMPLEMENT_COMPLETE` as documentation on prompt.ts:69) and the implementing-specs skill text (which also contains this string). Since COMPLETE is checked before HANDOFF, every session that loads the skill will falsely match as "complete." This is a critical bug that makes the multi-session loop non-functional.

- **Handoff continuity: unable to assess.** Only 1 session ran (due to false completion), so there's no session boundary to evaluate. The handoff file itself is high quality — precise slice state, file lists, remaining work, key patterns to follow.

- **Completion detection: fail.** False positive. The run stopped with 3 remaining slices. Linear shows ENG-2582, ENG-2583, ENG-2584 all still pending. The `checkSpecComplete` fallback (line 319-320) would have caught this if the signal detection hadn't short-circuited first.

- **Efficiency: unable to assess.** Run was truncated by the false completion bug, so sessions-per-slice ratio is meaningless.

- **Hook guards: partial failure.** The pre-commit TDD gate did NOT block commit `d5289983` (5 implementation files, 0 test files). This should have been caught. The commit size gate passed correctly (5 files < 8 max). Post-commit context rotation was not triggered (context was below threshold). The commit frequency PreToolUse guard status is unknown.

- **Observability: pass.** Manifest correctly recorded all events (with the wrong signal). Session ID is recorded. Handoff file preserved with timestamp.

**Collapse events:**
1. **False completion detection** — `detectSignal` matched `AUTO_IMPLEMENT_COMPLETE` from prompt/skill documentation text in the stream-json stdout, not from the agent's actual output signal. Run terminated prematurely with 3 slices unimplemented.
2. **TDD gate bypass** — Pre-commit hook failed to block an implementation-only commit. Either the hook wasn't installed, errored silently, or was overridden.

**Suggestions:**
- **Fix false completion detection (critical, blocks all multi-session runs):** The `detectSignal` function must NOT match signal strings that appear in prompt text or skill documentation. Options:
  1. **Structured signal:** Instead of searching raw stdout, have the agent write a signal to a file (e.g., `/tmp/auto-impl-signal.json` with `{"signal": "handoff"}`). This decouples signal detection from stdout content entirely.
  2. **Sentinel wrapping:** Use a unique delimiter the prompt/skill text won't contain, e.g., `###AUTO_IMPLEMENT_SIGNAL:COMPLETE###`. Change both the prompt and the detection regex.
  3. **Parse stream-json events:** Instead of `stdout.includes()`, parse the JSONL events and only check for signal strings in `assistant` message events of type `text` (not in tool results, system messages, or user messages that contain the prompt).
  Option 3 is most correct but complex. Option 1 is simplest and most robust.
- **Investigate TDD gate failure:** Run the pre-commit-guard.sh manually against the staged files from commit `d5289983` to reproduce. Check whether `installHooks` logs appeared in the session's stderr. Add logging to the hook itself (write to `/tmp/auto-impl-tdd-gate.log`) so failures can be diagnosed post-hoc.
- **Add a "this run was the 6th attempt" concern:** There were 6 runs for ENG-2004 on the same day. The previous 5 likely also hit the false completion bug. This means the auto-implement loop has been non-functional for multi-session specs. All runs complete after 1 session regardless of remaining work.
