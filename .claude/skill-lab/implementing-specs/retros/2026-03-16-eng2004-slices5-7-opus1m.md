### 2026-03-16 — Session cd69b434 (ENG-2004 slices 5-7: MCP tools, system prompt, error handling — Opus 1M)
**Verdict:** worked well
**Collapse events:** 0

**Key observations:**

- **Orient: pass.** Agent ran `plan show ENG-2004 --tree`, identified slices 1-4 as Done, picked up at ENG-2582 (slice 5). Read key reference files: `data_browse_tool.py` (MCP pattern), `unified_client.py` (client to extend), `agent.py` (wiring), `experiments/linear.py` (pagination logic to port), `agent_factories.py` (feature flag wiring), test patterns (`conftest`, existing tool tests). No excessive rewinds. Orient consumed ~12% of 1M context — compare to the Sonnet 200K first attempt (run `20260316_092305`) where orient alone consumed 60% and produced zero implementation.

- **TDD: pass — first clean TDD session on ENG-2004.** This is the breakthrough after two consecutive TDD collapses (2026-03-12 ENG-2580, 2026-03-13 ENG-2581). For each sub-component:
  - **UnifiedClient methods:** 26 tests written first → watched fail (`list_tasks` doesn't exist) → 245 lines implemented → all pass
  - **MCP tools:** 13 tests written first → watched fail (`ModuleNotFoundError`) → 473-line server implemented → fixed SDK structure bug → all pass
  - **Agent wiring:** TDD order guard *actually blocked* the agent from editing implementation before tests. Agent complied, wrote wiring test, then implemented. This is the hook working as designed.
  - **System prompt (slice 6):** Tests for `has_linear` and `linear_project_names` written first → watched fail → implemented
  - **Project/summary tools (slice 6):** Tests for `browse_linear_projects` and `linear_summary` written first → watched fail → implemented
  - **Error handling (slice 7):** 14 verification tests confirming defensive patterns already built in. Smart — verified existing behavior rather than adding unnecessary code.

- **TDD order guard working:** The PreToolUse hook caught the agent trying to edit implementation before writing tests (during the agent wiring step). Agent was blocked, wrote the test file, then proceeded. This is the structural enforcement suggested in the 2026-03-13 retro, now proven effective.

- **Commit frequency guard working:** Agent was blocked when >4 files were dirty (during slice 6 implementation). Committed the batch, then continued. Clean enforcement with no workarounds attempted.

- **Slice discipline: pass.** Three slices (ENG-2582, 2583, 2584) implemented in order. Each committed and pushed before the next. No scope creep. The agent recognized slice 7's error handling was mostly built in from slices 5-6 and wrote verification tests instead of redundant code.

- **Self-verification: pass.** Full test suite run after each slice: 1,664 → 1,683 → 1,697. Lint issues fixed. Rebase performed when remote had new commits. Push hooks respected (lint fixed, not bypassed).

- **Linear hygiene: pass.** `plan start` called for ENG-2582. `plan close` called for each of ENG-2582, ENG-2583, ENG-2584. Parent issue updated with completion status. Cross-slice verification done before signaling complete.

- **Context management: N/A (1M changes the game).** Final context: 19.4% of 1M (194K tokens used, 806K remaining). The same 194K would have been 97% of Sonnet's 200K window. No handoff needed. No context pressure decisions. The 50%/60% thresholds were never approached. *However:* `cctx` was broken during the run (hardcoded 200K for all models). It reported 60-90% when real context was 12-19%. The agent pushed through the "60% MUST hand off" threshold because it was mid-implementation — technically a process violation, but the right call given broken tooling. The director session fixed `cctx` mid-run.

- **Commit discipline: pass.** Multiple small commits across the session: UnifiedClient methods (690 insertions), MCP server (1,023 insertions), agent wiring, lint fixes, system prompt + tools, error handling tests. Each committed before starting the next sub-component.

**What made this session succeed where the previous two failed:**

1. **Opus 1M context** — 119K of orient tokens was 12% not 60%. Three slices fit in one session with 80% headroom remaining.
2. **TDD order guard (PreToolUse hook)** — Structurally enforced tests-first, blocking implementation file edits until a test file existed since the last commit. This was the exact fix suggested in the 2026-03-13 retro.
3. **Commit frequency guard** — Prevented accumulation of dirty files, forcing small commits throughout.

**Suggestions:**
- **`cctx` must know about 1M models.** The broken `cctx` was a near-miss — if the agent had obediently handed off at "60%" (really 12%), the session would have been another orient-only waste. The fix was applied mid-run but should have been in place before the run. Recommendation: `cctx` should read the model's context window from the session metadata, not a hardcoded lookup table.
- **Context thresholds need recalibration for 1M.** 50% of 1M = 500K tokens — that's enormous. The thresholds were designed for 200K. For 1M, the conservative "stop at 50%" rule means leaving 500K tokens unused. Consider absolute-token thresholds (e.g., "hand off when <100K remaining") rather than percentage-based ones.
- **No communication signals in headless mode — by design.** The lab notes this as a known limitation. No change needed.
