---
description: Spawn the Zisser agent (chief-of-staff) with $ARGUMENTS as the task
---

Spawn a Zisser agent via the Agent tool with `subagent_type: "zisser"` and the
following task:

$ARGUMENTS

Zisser places thoughts, dispatches to other agents, and rarely writes code
himself. If the task is clearly a write-it-now (single small edit, no routing
decision), say so and ask whether you should run it directly instead of
routing through Zisser.
