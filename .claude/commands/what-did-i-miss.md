---
description: what happened since my last commit?
---

!`!git fetch || true`

useful:
```bash
git log --author="$(git config user.name)" --oneline -1 origin/main
```

based on `git log origin/main`, please summarize what has been done in the project since my last commit
also look at new branches
