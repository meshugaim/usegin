{
  "name": "spotlight",
  "description": "Query local Spotlight (Sentry dev) traces, errors, and logs",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "spotlight-cli": "./src/cli.ts"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.2"
  }
}
