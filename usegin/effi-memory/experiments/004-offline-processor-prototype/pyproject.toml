[project]
name = "exp004-offline-processor-prototype"
version = "0.0.1"
description = "Single-topic prototype of the effi-memory offline processor"
requires-python = ">=3.11"
dependencies = [
    "python-frontmatter>=1.1.0",
]

[tool.uv]
package = false
